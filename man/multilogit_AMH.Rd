% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multilogit_AMH.R
\name{multilogit_AMH}
\alias{multilogit_AMH}
\title{Adaptive Metropolis-Hastings Multinomial Logistic Regression}
\usage{
multilogit_AMH(
  Y,
  X,
  n_sample = 1000L,
  n_burn = 200L,
  n_sigma_check = 20L,
  step_size = 0.1,
  acceptance_lower = 0.2,
  acceptance_upper = 0.4,
  step_increase = 2,
  step_decrease = 0.9,
  prior_mean = NULL,
  prior_var = NULL,
  reference_cat = NULL,
  probs = FALSE,
  progress = TRUE
)
}
\arguments{
\item{Y}{An N by C numeric matrix of non-negative category counts. Every row
must have a positive total.}

\item{X}{An N by P numeric design matrix. The first column should generally
be an intercept column of ones.}

\item{n_sample}{Positive integer number of retained posterior draws.}

\item{n_burn}{Non-negative integer number of burn-in iterations.}

\item{n_sigma_check}{Positive integer adaptation-window length.}

\item{step_size}{Finite positive initial random-walk standard deviation.}

\item{acceptance_lower}{Lower target acceptance rate in `[0, 1)`.}

\item{acceptance_upper}{Upper target acceptance rate in `(0, 1]`; must be
greater than `acceptance_lower`.}

\item{step_increase}{Multiplicative proposal-scale increase greater than one.}

\item{step_decrease}{Multiplicative proposal-scale decrease strictly between
zero and one.}

\item{prior_mean}{Optional finite numeric vector with one value per predictor.
Defaults to zero.}

\item{prior_var}{Optional positive-definite P by P prior covariance matrix.
Defaults to the identity matrix.}

\item{reference_cat}{Optional integer reference category. Its coefficients
are held at zero. Defaults to the last category.}

\item{probs}{Logical; if TRUE, return fitted probabilities for every retained
draw.}

\item{progress}{Logical; if TRUE, print periodic progress updates.}
}
\value{
A list with `posterior_coef`, a P by C by `n_sample` array;
`acceptance_rate`, the retained-sampling acceptance rate for each
coefficient; and `final_step_size`, the post-adaptation proposal scales. If
`probs = TRUE`, the list also contains `posterior_prob`, an N by C by
`n_sample` array. Diagnostics for the reference category are `NA`.
}
\description{
Fits a Bayesian multinomial logistic regression using coordinate-wise
random-walk Metropolis-Hastings proposals. Proposal standard deviations are
adapted during burn-in and fixed while posterior draws are retained. Unlike
the package's other samplers, this method targets the multinomial likelihood
directly and does not introduce augmentation variables.
}
\examples{
X <- cbind(1, scale(iris[, 1:4]))
Y <- model.matrix(~ Species - 1, data = iris)
fit <- multilogit_AMH(
  Y, X, n_sample = 20, n_burn = 10,
  probs = TRUE, progress = FALSE
)

}
